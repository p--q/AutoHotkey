クリップボードからプレインテキストを取得してプレインテキストのみをクリップボードに返す

Winキー＋Alt＋S  // 出力に用法がない

すべて半角に変換
if
行頭に「商品名」があるときそれはDI情報
DI情報について
「商品名」とそれに続く空白を削除。






Winキー＋Alt＋D  // 出力に用法がある
すべて半角に変換
if
先頭行が「--」から始まるときそれは外来処方オーダー
外来処方オーダーについて
「--」「<R」「処方箋期限」から始まる行と空行は削除。
「\d+\S+分$」にマッチする文字列を削除。
「\d+\S+[錠pg枚ﾄ]$」にマッチする文字列の前に「@@SPACE@@」を挿入。
「^\S+時」にマッチする文字列は上の行に結合する
「cap$」にマッチする文字列を「c」に置換。
「^分/d+\s\d」にマッチする文字列の\sを「@@SPACE@@」に置換する
すべての空白を削除。

「^分[123]\S+」にマッチする文字列について
「毎(?=.)」と「食後」は削除、
「(?:と)?眠前」を「寝」に置換
「食前」を「前」に置換
「\[食間\]」は削除
「1日\d回」を削除
上の行と結合する

「^分\d\S+」にマッチする文字列について
「(?:と)?眠前」を「寝」に置換
「\[食間\]」は削除
「1日\d回」を削除
上の行と結合する

else

先頭行が「処方日」から始まるときそれは入院処方オーダー
入院処方オーダーについて
「処方日」から始まる行の次の行から次の「処方日」から始まる行または最終行までを一つの処方として取得。
一つの処方について







「@@SPACE@@」を半角スペースに置換する

